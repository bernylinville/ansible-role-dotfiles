---
- name: Ensure dotfiles repository is cloned locally.
  git:
    repo: "{{ dotfiles_repo }}"
    dest: "{{ dotfiles_repo_local_destination }}"
    version: "{{ dotfiles_repo_version }}"
    accept_hostkey: "{{ dotfiles_repo_accept_hostkey }}"
  become: false

# =============================================================================
# Stow 模式
# =============================================================================
- name: Ensure stow is installed.
  when: dotfiles_use_stow
  block:
    - name: Check if stow is available.
      command: which stow
      register: stow_check
      failed_when: false
      changed_when: false

    - name: Fail if stow is not installed.
      fail:
        msg: >
          GNU Stow is not installed. Please install it first:
          macOS: brew install stow
          Arch Linux: pacman -S stow / paru -S stow
      when: stow_check.rc != 0

- name: Deploy dotfiles via stow.
  when: dotfiles_use_stow
  block:
    - name: Backup existing dotfiles before stow.
      shell: |
        for pkg in {{ dotfiles_stow_packages | join(' ') }}; do
          for file in $(find "{{ dotfiles_repo_local_destination }}/${pkg}" -maxdepth 1 -name '.*' -type f -exec basename {} \;); do
            target="{{ dotfiles_home }}/${file}"
            if [ -f "$target" ] && [ ! -L "$target" ]; then
              mv "$target" "${target}.bak.$(date +%Y%m%d%H%M%S)"
            fi
          done
        done
      args:
        executable: /bin/bash
      changed_when: false
      become: false

    - name: Install dotfiles packages via stow.
      command: >
        stow -d {{ dotfiles_repo_local_destination }}
        -t {{ dotfiles_home }}
        --restow {{ item }}
      with_items: "{{ dotfiles_stow_packages }}"
      become: false

# =============================================================================
# 传统模式（向后兼容）
# =============================================================================
- name: Deploy dotfiles via symlink (legacy mode).
  when: not dotfiles_use_stow
  block:
    - name: Ensure all configured dotfiles are links.
      command: "ls -F {{ dotfiles_home }}/{{ item }}"
      register: existing_dotfile_info
      failed_when: false
      check_mode: false
      changed_when: false
      with_items: "{{ dotfiles_files }}"

    - name: Remove existing dotfiles file if a replacement is being linked.
      file:
        path: "{{ dotfiles_home }}/{{ dotfiles_files[item.0] }}"
        state: absent
      when: "'@' not in item.1.stdout"
      with_indexed_items: "{{ existing_dotfile_info.results }}"

    - name: Ensure parent folders of link dotfiles exist.
      file:
        path: "{{ (dotfiles_home ~ '/' ~ item) | dirname }}"
        state: directory
        follow: true
      become: false
      with_items: "{{ dotfiles_files }}"

    - name: Link dotfiles into home folder.
      file:
        src: "{{ dotfiles_repo_local_destination }}/{{ item }}"
        dest: "{{ dotfiles_home }}/{{ item }}"
        state: link
      become: false
      with_items: "{{ dotfiles_files }}"
